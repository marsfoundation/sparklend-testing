pool.setUserEmode()
├── when categoryId is not zero liquidationThreshold for categoryId is zero
   └── it should revert  
├── when user has an active borrow with another eMode categoryId
   └── it should revert
├── when user is changing from an active eMode and their HF is below 1
   └── it should revert (boundary)
├── when the user is changing from no eMode to active eMode
   └── it should succeed
├── when the user is changing from active eMode to different eMode with same ID
   └── it should succeed
├── when the user is changing from active eMode to different eMode with different ID
   └── it should succeed
└── when the user is changing from active eMode to no eMode
   └── it should succeed
